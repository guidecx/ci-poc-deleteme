name: CICD (Staging, Prod)

on:
  push:
    branches:
      - main
    paths-ignore:
      - .gitignore
      - LICENSE
      - README.md

  pull_request:
    branches:
      - main

  merge_group:
    types: [checks_requested]

  workflow_dispatch:

permissions:
  id-token: write
  contents: read

defaults:
  run:
    shell: pwsh

jobs:
  
  build:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/build.yml
    secrets: inherit

  unit-test:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/unit-test.yml
    secrets: inherit

  # Merge queue needs a green check; tests already ran on the PR (see branch protection note in README).
  merge-queue-pass:
    if: github.event_name == 'merge_group'
    runs-on: ubuntu-latest
    steps:
      - run: write-host "Merge queue gate (no duplicate build/test)."

  staging-deploy:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/deploy-environment.yml
    with:
      environment: staging
    secrets: inherit

  prod-approval-gate:
    needs: staging-deploy
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    environment: prod
    runs-on: ubuntu-latest
    steps:
      - name: approved
        env: 
          ENV_NAME: prod
        run: write-host "Approve for $($env.ENV_NAME)"
    
  prod-deploy:
    needs: prod-approval-gate
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/deploy-environment.yml
    with:
      environment: prod
    secrets: inherit